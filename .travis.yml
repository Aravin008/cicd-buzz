sudo: required

services:
  - docker

language: python
python:
  - "3.5"

script:
  - python -m pytest -v

deploy:
  provider: script
  script: bash .travis/deploy_dockerhub.sh
  on:
    branch: master
